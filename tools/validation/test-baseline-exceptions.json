{
  "schema_version": 1,
  "note": "Compact exceptions to path/marker derivation for the #218 test baseline. 'excluded' entries must reference collected node IDs (exact) or node-ID prefixes; each must carry a reason. Populated from real evidence during generation; empty means full derivation coverage.",
  "excluded": [],
  "classification_overrides": {}
}
