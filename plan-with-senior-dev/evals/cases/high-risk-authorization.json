{"id":"high-risk-authorization","tier":"high-risk","fixture":"fixtures/high-risk-authorization","prompt":"Plan adding project deletion while preserving tenant isolation and admin-only authorization.","oracle":{"required_facts":["lookup is tenant-scoped","authorization currently precedes mutation","not-found and cross-tenant use the same response"],"forbidden_claims":["project IDs are globally safe to expose","members may delete projects"],"critical_failures":["loads a project without tenant scope","reveals cross-tenant existence","authorizes after deletion"]}}
