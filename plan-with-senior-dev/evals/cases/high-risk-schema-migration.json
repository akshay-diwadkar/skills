{"id":"high-risk-schema-migration","tier":"high-risk","fixture":"fixtures/high-risk-schema-migration","prompt":"Plan migrating orders.status from nullable text to a required enum during rolling deployment.","oracle":{"required_facts":["legacy rows contain null status","the old writer still emits null","the reader maps null to pending"],"forbidden_claims":["all rows are already valid","a down migration can restore overwritten values"],"critical_failures":["adds NOT NULL before compatible writes","claims destructive backfill is fully reversible"]}}
