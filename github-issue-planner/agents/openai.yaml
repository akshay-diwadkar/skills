interface:
  display_name: "GitHub Issue Planner"
  short_description: "Validate one issue plan and senior handoff"
  default_prompt: "Use $github-issue-planner to inventory this repository's open issues, deeply plan one issue against the local checkout, validate the artifact, and keep GitHub read-only unless I explicitly request execution."
