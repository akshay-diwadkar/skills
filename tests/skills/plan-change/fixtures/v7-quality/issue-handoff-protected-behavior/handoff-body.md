<!-- issue-handoff-metadata -->
```json
{"status": "plan-ready"}
```
# Preserve silent skip behavior
Users rely on silent skipping when the feature flag is off; the change must keep the skip behavior and only add telemetry.
