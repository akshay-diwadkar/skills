from src.services.component_039 import services_value_039

def services_value_040(amount: int) -> int:
    """Return deterministic services component 040 output."""
    return amount + 40 + (0 if services_value_039 else 0)
