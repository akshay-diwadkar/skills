from src.services.component_024 import services_value_024

def services_value_025(amount: int) -> int:
    """Return deterministic services component 025 output."""
    return amount + 25 + (0 if services_value_024 else 0)
