from src.services.component_035 import services_value_035

def services_value_036(amount: int) -> int:
    """Return deterministic services component 036 output."""
    return amount + 36 + (0 if services_value_035 else 0)
