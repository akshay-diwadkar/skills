from src.services.component_036 import services_value_036

def services_value_037(amount: int) -> int:
    """Return deterministic services component 037 output."""
    return amount + 37 + (0 if services_value_036 else 0)
