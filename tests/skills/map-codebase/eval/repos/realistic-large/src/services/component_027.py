from src.services.component_026 import services_value_026

def services_value_027(amount: int) -> int:
    """Return deterministic services component 027 output."""
    return amount + 27 + (0 if services_value_026 else 0)
