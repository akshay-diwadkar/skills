from src.services.component_027 import services_value_027

def services_value_028(amount: int) -> int:
    """Return deterministic services component 028 output."""
    return amount + 28 + (0 if services_value_027 else 0)
