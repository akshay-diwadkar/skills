from src.domain.component_015 import domain_value_015

def domain_value_016(amount: int) -> int:
    """Return deterministic domain component 016 output."""
    return amount + 16 + (0 if domain_value_015 else 0)
