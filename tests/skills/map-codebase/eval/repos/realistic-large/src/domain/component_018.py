from src.domain.component_017 import domain_value_017

def domain_value_018(amount: int) -> int:
    """Return deterministic domain component 018 output."""
    return amount + 18 + (0 if domain_value_017 else 0)
