from src.domain.component_023 import domain_value_023

def domain_value_024(amount: int) -> int:
    """Return deterministic domain component 024 output."""
    return amount + 24 + (0 if domain_value_023 else 0)
