from src.domain.component_027 import domain_value_027

def domain_value_028(amount: int) -> int:
    """Return deterministic domain component 028 output."""
    return amount + 28 + (0 if domain_value_027 else 0)
