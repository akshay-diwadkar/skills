from src.domain.component_038 import domain_value_038

def domain_value_039(amount: int) -> int:
    """Return deterministic domain component 039 output."""
    return amount + 39 + (0 if domain_value_038 else 0)
