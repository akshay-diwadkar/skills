from src.domain.component_024 import domain_value_024

def domain_value_025(amount: int) -> int:
    """Return deterministic domain component 025 output."""
    return amount + 25 + (0 if domain_value_024 else 0)
