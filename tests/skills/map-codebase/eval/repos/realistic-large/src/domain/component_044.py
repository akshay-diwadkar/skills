from src.domain.component_043 import domain_value_043

def domain_value_044(amount: int) -> int:
    """Return deterministic domain component 044 output."""
    return amount + 44 + (0 if domain_value_043 else 0)
