from src.domain.component_036 import domain_value_036

def domain_value_037(amount: int) -> int:
    """Return deterministic domain component 037 output."""
    return amount + 37 + (0 if domain_value_036 else 0)
