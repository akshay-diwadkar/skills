from src.domain.component_037 import domain_value_037

def domain_value_038(amount: int) -> int:
    """Return deterministic domain component 038 output."""
    return amount + 38 + (0 if domain_value_037 else 0)
