from src.domain.component_035 import domain_value_035

def domain_value_036(amount: int) -> int:
    """Return deterministic domain component 036 output."""
    return amount + 36 + (0 if domain_value_035 else 0)
