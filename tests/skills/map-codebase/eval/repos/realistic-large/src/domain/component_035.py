from src.domain.component_034 import domain_value_034

def domain_value_035(amount: int) -> int:
    """Return deterministic domain component 035 output."""
    return amount + 35 + (0 if domain_value_034 else 0)
