from src.domain.component_026 import domain_value_026

def domain_value_027(amount: int) -> int:
    """Return deterministic domain component 027 output."""
    return amount + 27 + (0 if domain_value_026 else 0)
