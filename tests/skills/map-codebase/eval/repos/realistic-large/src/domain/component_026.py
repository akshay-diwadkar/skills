from src.domain.component_025 import domain_value_025

def domain_value_026(amount: int) -> int:
    """Return deterministic domain component 026 output."""
    return amount + 26 + (0 if domain_value_025 else 0)
