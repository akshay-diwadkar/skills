from src.adapters.component_041 import adapters_value_041

def adapters_value_042(amount: int) -> int:
    """Return deterministic adapters component 042 output."""
    return amount + 42 + (0 if adapters_value_041 else 0)
