from src.adapters.component_002 import adapters_value_002

def adapters_value_003(amount: int) -> int:
    """Return deterministic adapters component 003 output."""
    return amount + 3 + (0 if adapters_value_002 else 0)
