from src.adapters.component_024 import adapters_value_024

def adapters_value_025(amount: int) -> int:
    """Return deterministic adapters component 025 output."""
    return amount + 25 + (0 if adapters_value_024 else 0)
