from src.adapters.component_014 import adapters_value_014

def adapters_value_015(amount: int) -> int:
    """Return deterministic adapters component 015 output."""
    return amount + 15 + (0 if adapters_value_014 else 0)
