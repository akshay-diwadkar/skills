from src.adapters.component_021 import adapters_value_021

def adapters_value_022(amount: int) -> int:
    """Return deterministic adapters component 022 output."""
    return amount + 22 + (0 if adapters_value_021 else 0)
