from src.adapters.component_007 import adapters_value_007

def adapters_value_008(amount: int) -> int:
    """Return deterministic adapters component 008 output."""
    return amount + 8 + (0 if adapters_value_007 else 0)
