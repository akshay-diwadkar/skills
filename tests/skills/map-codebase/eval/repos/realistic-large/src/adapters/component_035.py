from src.adapters.component_034 import adapters_value_034

def adapters_value_035(amount: int) -> int:
    """Return deterministic adapters component 035 output."""
    return amount + 35 + (0 if adapters_value_034 else 0)
