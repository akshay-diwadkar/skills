from src.adapters.component_026 import adapters_value_026

def adapters_value_027(amount: int) -> int:
    """Return deterministic adapters component 027 output."""
    return amount + 27 + (0 if adapters_value_026 else 0)
