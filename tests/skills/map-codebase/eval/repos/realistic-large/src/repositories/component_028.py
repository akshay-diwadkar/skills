from src.repositories.component_027 import repositories_value_027

def repositories_value_028(amount: int) -> int:
    """Return deterministic repositories component 028 output."""
    return amount + 28 + (0 if repositories_value_027 else 0)
