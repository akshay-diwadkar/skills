from src.repositories.component_040 import repositories_value_040

def repositories_value_041(amount: int) -> int:
    """Return deterministic repositories component 041 output."""
    return amount + 41 + (0 if repositories_value_040 else 0)
