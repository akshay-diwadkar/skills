from src.repositories.component_036 import repositories_value_036

def repositories_value_037(amount: int) -> int:
    """Return deterministic repositories component 037 output."""
    return amount + 37 + (0 if repositories_value_036 else 0)
