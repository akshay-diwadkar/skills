from src.repositories.component_022 import repositories_value_022

def repositories_value_023(amount: int) -> int:
    """Return deterministic repositories component 023 output."""
    return amount + 23 + (0 if repositories_value_022 else 0)
