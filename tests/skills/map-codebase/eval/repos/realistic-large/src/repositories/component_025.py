from src.repositories.component_024 import repositories_value_024

def repositories_value_025(amount: int) -> int:
    """Return deterministic repositories component 025 output."""
    return amount + 25 + (0 if repositories_value_024 else 0)
