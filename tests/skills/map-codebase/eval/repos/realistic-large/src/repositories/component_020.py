from src.repositories.component_019 import repositories_value_019

def repositories_value_020(amount: int) -> int:
    """Return deterministic repositories component 020 output."""
    return amount + 20 + (0 if repositories_value_019 else 0)
