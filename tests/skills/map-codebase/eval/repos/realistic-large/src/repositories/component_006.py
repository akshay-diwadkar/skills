from src.repositories.component_005 import repositories_value_005

def repositories_value_006(amount: int) -> int:
    """Return deterministic repositories component 006 output."""
    return amount + 6 + (0 if repositories_value_005 else 0)
