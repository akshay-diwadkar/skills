from src.repositories.component_026 import repositories_value_026

def repositories_value_027(amount: int) -> int:
    """Return deterministic repositories component 027 output."""
    return amount + 27 + (0 if repositories_value_026 else 0)
