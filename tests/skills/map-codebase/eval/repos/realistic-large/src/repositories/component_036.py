from src.repositories.component_035 import repositories_value_035

def repositories_value_036(amount: int) -> int:
    """Return deterministic repositories component 036 output."""
    return amount + 36 + (0 if repositories_value_035 else 0)
