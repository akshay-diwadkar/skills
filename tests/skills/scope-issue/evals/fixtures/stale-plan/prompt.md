Use $scope-issue in single-issue mode to evaluate execution readiness for issue #27 using an artifact whose source issue timestamp and checkout commit are older than the supplied fresh issue JSON and current HEAD. The snapshot digest and checkout anchors will not match; report the artifact as invalid with the checker diagnostics rather than sealing a blocked handoff. Do not create a branch.
