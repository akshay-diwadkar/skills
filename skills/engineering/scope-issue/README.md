# Issue-to-Plan Pipeline

`scope-issue` reconciles issue claims with the checkout and can pass its
issue-derived artifact to `plan-change` with `prepare_plan.py --request-file`.
See the canonical [design-to-plan pipeline](../design-codebase/README.md).
