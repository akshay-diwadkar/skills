# Build Codebase Knowledge

This skill builds compact, deterministic repository-navigation artifacts under `.agent/knowledge/`. Use `status`, `refresh`, and the phase-scoped `resolve` command to avoid broad exploration.

Build writes only to the configured knowledge directory. `link-docs` and `generate-workflow` are independent opt-in commands.
