# Plan Change Pipeline

`plan-change` accepts request files produced from issue scoping or codebase
design decisions. See the canonical
[design-to-plan pipeline](../design-codebase/README.md), including the exact
guarantees and limitations of `inventory.json`'s `request_sha256`.
